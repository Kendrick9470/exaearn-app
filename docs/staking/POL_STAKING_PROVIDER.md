# POL Staking Provider

Required before readiness: correct Polygon staking contracts, validator selection, delegation, reward claiming, commission, unbonding, withdraw claim, checkpoint/epoch handling, POL token handling, event indexing, Ethereum gas accounting where required.
