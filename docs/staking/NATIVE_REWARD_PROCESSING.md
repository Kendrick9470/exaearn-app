# Native Reward Processing

Displayed APY is informational only and must never create reward credits. Native reward batches must be created from verified blockchain/provider settlement data, then allocated by active principal and exact network period.

Current status: database structures exist; distribution jobs remain blocked until provider integrations prove reward source data.
