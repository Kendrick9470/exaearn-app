# AVAX Staking Provider

Required before readiness: supported validator/delegated staking flow, minimum amount, fixed staking duration, start/end time validation, transaction submission, reward settlement at completion, principal release, validator failure handling.
