{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "api": "php -d extension=gd -d extension=pdo_pgsql -d extension=pgsql artisan serve",
        "queue": "php -d extension=gd -d extension=pdo_pgsql -d extension=pgsql artisan queue:listen --tries=1 --timeout=0",
        "logs": "php artisan pail --timeout=0",
        "build": "vite build",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
